import CodeBlock from '@theme/CodeBlock'

Ejemplos

| Clase                                               | Descripcion                                                                                                                                        |
| --------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| <CodeBlock language="css">.sm:pt-2</CodeBlock>      | Agrega un `padding-top` de `space 2` cuando la pantalla es mayor a `sm`                                                                            |
| <CodeBlock language="css">pb-3 .lg:pb-2</CodeBlock> | Agrega un `padding-bottom` de `space 3` para pantallas `mobile` y cuando la pantalla es mayor a `lg` configura el `padding-bottom` de `space de 2` |
