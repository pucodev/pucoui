import CodeBlock from '@theme/CodeBlock'

Ejemplos

| Clase                                                       | Descripcion                                                                                                  |
| ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| <CodeBlock language="css">.md:is-hidden</CodeBlock>         | Oculta el elemento (`display: none`) para pantallas mayores a `md`                                           |
| <CodeBlock language="css">is-hidden .sm:is-flex</CodeBlock> | Oculta el elemento para pantallas `mobile` y para pantallas mayor a `md` se vuelve un flex (`display: flex`) |
