import PreviewElement from '@site/src/components/PreviewElement'
import CodeBlock from '@theme/CodeBlock'
import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'

import TableBasic from '@site/src/components/html/table/TableBasic'
import TableBasicCode from '@site/src/components/generated/table/TableBasic.html?raw'

import TableBordered from '@site/src/components/html/table/TableBordered'
import TableBorderedCode from '@site/src/components/generated/table/TableBordered.html?raw'

import TableStripped from '@site/src/components/html/table/TableStripped'
import TableStrippedCode from '@site/src/components/generated/table/TableStripped.html?raw'

import TableStrippedColor from '@site/src/components/html/table/TableStrippedColor'
import TableStrippedColorCode from '@site/src/components/generated/table/TableStrippedColor.html?raw'

import TableHoverable from '@site/src/components/html/table/TableHoverable'
import TableHoverableCode from '@site/src/components/generated/table/TableHoverable.html?raw'

import TableHoverableColor from '@site/src/components/html/table/TableHoverableColor'
import TableHoverableColorCode from '@site/src/components/generated/table/TableHoverableColor.html?raw'

import TableSizes from '@site/src/components/html/table/TableSizes'
import TableSizesCode from '@site/src/components/generated/table/TableSizes.html?raw'

# Table

Displays your information sorted in tables.

## Structure

- `table`: Container tag
  - `thead`: Table header (optional)
  - `tbody`: Table body
    - `tr`: Table row
      - `th`: Header cell
      - `td`: General cell

## Basic Usage

<PreviewElement>
  <div className="dialog-preview">
    <div className="is-bg-white">
      <TableBasic />
    </div>
  </div>
</PreviewElement>

<CodeBlock language="html">{TableBasicCode}</CodeBlock>

## Bordered

Displays a table with all elements bordered using the `is-bordered` class along with the `table` class.

<Tabs>
  <TabItem value="preview" label="Preview">
    <PreviewElement>
      <div className="is-bg-white">
        <TableBordered />
      </div>
    </PreviewElement>
  </TabItem>

  <TabItem value="code" label="Code">
    <CodeBlock language="html">{TableBorderedCode}</CodeBlock>
  </TabItem>
</Tabs>

## Striped

To display odd rows with a neutral color, use the `is-striped` class.

<Tabs>
  <TabItem value="preview" label="Preview">
    <PreviewElement>
      <div className="is-bg-white">
        <TableStripped />
      </div>
    </PreviewElement>
  </TabItem>

  <TabItem value="code" label="Code">
    <CodeBlock language="html">{TableStrippedCode}</CodeBlock>
  </TabItem>
</Tabs>

### Colors

To display the striped rows with another color, use the `is-{COLOR}` class, where `COLOR` is one of the color values. For more information, visit the [color customization documentation](../customize/color).

<Tabs>
  <TabItem value="preview" label="Preview">
    <PreviewElement>
      <TableStrippedColor />
    </PreviewElement>
  </TabItem>

  <TabItem value="code" label="Code">
    <CodeBlock language="html">{TableStrippedColorCode}</CodeBlock>
  </TabItem>
</Tabs>

## Highlight on Hover

To display a neutral color when hovering over a row, you can use the `is-hoverable` class along with the `table` class.

<Tabs>
  <TabItem value="preview" label="Preview">
    <PreviewElement>
      <div className="is-bg-white">
        <TableHoverable />
      </div>
    </PreviewElement>
  </TabItem>

  <TabItem value="code" label="Code">
    <CodeBlock language="html">{TableHoverableCode}</CodeBlock>
  </TabItem>
</Tabs>

### Colors

To display the row with another color on hover, use the `is-{COLOR}` class, where `COLOR` is one of the color values. For more information, visit the [color customization documentation](../customize/color).

<Tabs>
  <TabItem value="preview" label="Preview">
    <PreviewElement>
      <div className="is-bg-white">
        <TableHoverableColor />
      </div>
    </PreviewElement>
  </TabItem>

  <TabItem value="code" label="Code">
    <CodeBlock language="html">{TableHoverableColorCode}</CodeBlock>
  </TabItem>
</Tabs>

## Sizes

Displays tables with different paddings using the `is-sm`, `is-md` (default), and `is-lg` classes along with the `table` class.

<Tabs>
  <TabItem value="preview" label="Preview">
    <PreviewElement>
      <TableSizes />
    </PreviewElement>
  </TabItem>

  <TabItem value="code" label="Code">
    <CodeBlock language="html">{TableSizesCode}</CodeBlock>
  </TabItem>
</Tabs>

## Responsive Table

If you want the table to have a horizontal scroll, you need to add a container with the `table-responsive` class.

export const tableHeaders = Array.from({ length: 20 }, (x, index) => `Header`)

<Tabs>
  <TabItem value="preview" label="Preview">
    <PreviewElement>
      <div className="table-responsive">
        <table className="table is-bg-white">
          <thead>
            <tr>
              {tableHeaders.map((header, index) => (
                <th key={index}>{header}</th>
              ))}
            </tr>
          </thead>
          <tbody>
            <tr>
              {tableHeaders.map((header, index) => (
                <td key={index}>{`Cell ${index + 1}`}</td>
              ))}
            </tr>
            <tr>
              {tableHeaders.map((header, index) => (
                <td key={index}>{`Cell ${index + 1}`}</td>
              ))}
            </tr>
            <tr>
              {tableHeaders.map((header, index) => (
                <td key={index}>{`Cell ${index + 1}`}</td>
              ))}
            </tr>
          </tbody>
        </table>
      </div>
    </PreviewElement>
  </TabItem>

  <TabItem value="code" label="Code">
    <CodeBlock language="html">
{`<div class="table-responsive">
    <table class="table">
      ...
    </table>
</div>`}
    </CodeBlock>
  </TabItem>
</Tabs>
