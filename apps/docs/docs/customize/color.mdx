import { colors } from '@site/src/components/variables'
import PreviewElement from '@site/src/components/PreviewElement'
import CodeBlock from '@theme/CodeBlock'

# Colors

A set of colors is managed in CSS variables using the `--pui-color-{color-name}` pattern.

## Color Table

<table>
<thead>
  <tr>
    <th>Color</th>
    <th>Preview</th>
    <th>Variable</th>
  </tr>
</thead>

  <tbody>
    {colors.map(color => (
      <tr>
        <td>{color}</td>
        <td>
          <PreviewElement>
            <div style={{width: '80px', height: '80px', borderRadius: '4px'}} className={`is-bg-${color}`}></div>
          </PreviewElement>
        </td>
        <td><CodeBlock>--pui-color-{color}</CodeBlock></td>
      </tr>
    ))}

  </tbody>
</table>
