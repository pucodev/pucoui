import { textSizes, defaultDimen } from '@site/src/components/variables'
import CodeBlock from '@theme/CodeBlock'
import PreviewElement from '@site/src/components/PreviewElement'

# Font Sizes

## Font Size Table

Pixel calculations are based on <code>{defaultDimen}px</code>.

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Size</th>
      <th>Pixels</th>
      <th>Variable</th>
      <th>Preview</th>
    </tr>
  </thead>
  <tbody>
    {textSizes.map((dimen, idx) => (
      <tr>
        <td>{idx + 1}</td>
        <td>{dimen}rem</td>
        <td>{dimen * defaultDimen}px</td>
        <td>
          <CodeBlock>--pui-font-size-{idx + 1}</CodeBlock>
        </td>
        <td>
          <PreviewElement>
            <div className={`is-font-size-${idx + 1}`}>Lorem ipsum</div>
          </PreviewElement>
        </td>
      </tr>
    ))}
  </tbody>
</table>
