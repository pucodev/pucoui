import PreviewElement from '@site/src/components/PreviewElement'
import CodeBlock from '@theme/CodeBlock'
import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'

import { imageAspectRatios, imageSizes } from '@site/src/components/variables'
import ImageBase from '@site/src/components/docs/ImageBase'

import ImageExample from '@site/src/components/html/image/ImageExample'
import ImageExampleCode from '@site/src/components/generated/image/ImageExample.html?raw'

import ImageRounded from '@site/src/components/html/image/ImageRounded'
import ImageRoundedCode from '@site/src/components/generated/image/ImageRounded.html?raw'

import ImageFallback from '@site/src/components/html/image/ImageFallback'
import ImageFallbackCode from '@site/src/components/generated/image/ImageFallback.html?raw'

# Image

Set sizes and aspect-ratios for your images.

## Basic Usage

<PreviewElement>
  <ImageExample />
</PreviewElement>

<CodeBlock language="html">{ImageExampleCode}</CodeBlock>

## Sizes

Available sizes:

<table>
  <thead>
    <tr>
      <th>Class</th>
      <th>Size</th>
      <th>Preview</th>
    </tr>
  </thead>

  <tbody>
    {imageSizes.map(size => (
      <tr>
        <td><CodeBlock>is-size-{size}</CodeBlock></td>
        <td>
          {size}px x {size}px
        </td>
        <td>
          <PreviewElement>
            <ImageBase image={2} size={size} />
          </PreviewElement>
        </td>
      </tr>
    ))}

  </tbody>
</table>

## Rounded

To round images, you can use the dimension helpers with the `is-rounded-*` classes.

Where `*` can be from 0 to 6.

If you need the image to be completely rounded, you can use the `is-rounded` class.

<Tabs>
  <TabItem value="preview" label="Preview">
    <PreviewElement>
      <ImageRounded />
    </PreviewElement>
  </TabItem>

  <TabItem value="code" label="Code">
    <CodeBlock language="html">{ImageRoundedCode}</CodeBlock>
  </TabItem>
</Tabs>

## Fallback

Displays text below your image while it loads. For example, you can use it to place the user's initials.

<Tabs>
  <TabItem value="preview" label="Preview">
    <PreviewElement>
      <ImageFallback />
    </PreviewElement>
  </TabItem>

  <TabItem value="code" label="Code">
    <CodeBlock language="html">{ImageFallbackCode}</CodeBlock>
  </TabItem>
</Tabs>

## Aspect Ratio

Use these classes to set the aspect ratio of an image. These classes are responsive, meaning they will always maintain the aspect ratio even if the screen is small or large.

<table>
  <thead>
    <tr>
      <th>Class</th>
      <th>Size</th>
      <th>Preview</th>
    </tr>
  </thead>

  <tbody>
    {imageAspectRatios.map(ar => (
      <tr>
        <td><CodeBlock>is-{ar[0]}by{ar[1]}</CodeBlock></td>
        <td>
          {ar[0]} / {ar[1]}
        </td>
        <td>
          <PreviewElement>
            <ImageBase image={4} ratio={ar.join(' ')} style={{maxWidth: '200px'}} />
          </PreviewElement>
        </td>
      </tr>
    ))}

  </tbody>
</table>
