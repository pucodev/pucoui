import PreviewElement from '@site/src/components/PreviewElement'
import CodeBlock from '@theme/CodeBlock'
import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'

import { spaces } from '@site/src/components/variables'

import StackBasic from '@site/src/components/html/stack/StackBasic'
import StackBasicCode from '@site/src/components/generated/stack/StackBasic.html?raw'

import StackVertical from '@site/src/components/html/stack/StackVertical'
import StackVerticalCode from '@site/src/components/generated/stack/StackVertical.html?raw'

import StackHorizontal from '@site/src/components/html/stack/StackHorizontal'
import StackHorizontalCode from '@site/src/components/generated/stack/StackHorizontal.html?raw'

import StackGap from '@site/src/components/html/stack/StackGap'
import StackGapCode from '@site/src/components/generated/stack/StackGap.html?raw'

# Stack

Stack is a layout component that facilitates the vertical or horizontal organization of elements by applying consistent spacing between them. Its main goal is to simplify the alignment of items in a row or column without needing to write additional styles for spacing.

## Basic Usage

Use the `is-stack` class. It automatically generates a vertical organization and a spacing of **_{spaces[2]} rem_**.

<PreviewElement>
  <StackBasic />
</PreviewElement>

```html
<div class="is-stack">
  <div>Item 1</div>
  <div>Item 2</div>
  <div>Item 3</div>
</div>
```

## Directions

Use the following classes to define the direction, and the elements will always align to the center.

:::tip
If you need the elements to align to the start or end, you can use the [flex helper classes](/docs/helper/flexbox#align-items).
:::

### Vertical

Use the `is-vstack` class to organize elements vertically.

<Tabs>
  <TabItem value="preview" label="Preview">
    <PreviewElement>
      <StackVertical />
    </PreviewElement>
  </TabItem>

  <TabItem value="code" label="Code">
    <CodeBlock language="html">{StackVerticalCode}</CodeBlock>
  </TabItem>
</Tabs>

### Horizontal

Use the `is-hstack` class to organize elements horizontally.

<Tabs>
  <TabItem value="preview" label="Preview">
    <PreviewElement>
      <StackHorizontal />
    </PreviewElement>
  </TabItem>

  <TabItem value="code" label="Code">
    <CodeBlock language="html">{StackHorizontalCode}</CodeBlock>
  </TabItem>
</Tabs>

## Spacing

Use `is-gap-{size}` to add space between flex items. The size is a value between `0` and `6`.

:::info
For more information on spacing values, visit the [customize spacing documentation](../customize/spacing).
:::

<Tabs>
  <TabItem value="preview" label="Preview">
    <PreviewElement>
      <StackGap />
    </PreviewElement>
  </TabItem>

  <TabItem value="code" label="Code">
    <CodeBlock language="html">{StackGapCode}</CodeBlock>
  </TabItem>
</Tabs>
