import PreviewElement from '@site/src/components/PreviewElement'
import CodeBlock from '@theme/CodeBlock'
import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'

import DialogBasic from '@site/src/components/html/dialog/DialogBasic'
import DialogBasicCode from '@site/src/components/generated/dialog/DialogBasic.html?raw'

import DialogScroll from '@site/src/components/html/dialog/DialogScroll'
import DialogScrollCode from '@site/src/components/generated/dialog/DialogScroll.html?raw'

import DialogCentered from '@site/src/components/html/dialog/DialogCentered'
import DialogCenteredCode from '@site/src/components/generated/dialog/DialogCentered.html?raw'

import DialogOutlined from '@site/src/components/html/dialog/DialogOutlined'
import DialogOutlinedCode from '@site/src/components/generated/dialog/DialogOutlined.html?raw'

import DialogImage from '@site/src/components/html/dialog/DialogImage'
import DialogImageCode from '@site/src/components/generated/dialog/DialogImage.html?raw'

# Dialog

Displays overlay content such as forms, messages, or confirmations.

## Structure

A dialog consists of:

- `dialog`: Container
  - `dialog-content`: Dialog content container
    - `dialog-card`: Card container, you can use `card` classes. [More info here](./card)
      - `dialog-header`: Dialog header, where the title and close button go
      - `dialog-body`: Dialog content
      - `dialog-footer`: Dialog action area, you can add buttons

## Basic Usage

By default, the dialog is aligned to the top.

:::info
To display the dialog, the `is-active` class must be used.
:::

<PreviewElement>
  <div className="dialog-preview">
    <DialogBasic />
  </div>
</PreviewElement>

<CodeBlock language="html">{DialogBasicCode}</CodeBlock>

## Centered

To center the dialog on the screen, use the `dialog-centered` class along with the `dialog` class.

<Tabs>
  <TabItem value="preview" label="Preview">
    <PreviewElement>
      <div className="dialog-preview" style={{height: '600px'}}>
        <DialogCentered />
      </div>
    </PreviewElement>
  </TabItem>

  <TabItem value="code" label="Code">
    <CodeBlock language="html">{DialogCenteredCode}</CodeBlock>
  </TabItem>
</Tabs>

## Scrollable Body

To make the body of a dialog scrollable, use the `dialog-scrollable` class along with the `dialog` class.

<Tabs>
  <TabItem value="preview" label="Preview">
    <PreviewElement>
      <div className="dialog-preview" style={{height: '494px'}}>
        <DialogScroll />
      </div>
    </PreviewElement>
  </TabItem>

  <TabItem value="code" label="Code">
    <CodeBlock language="html">{DialogScrollCode}</CodeBlock>
  </TabItem>
</Tabs>

## Card Classes

You can use all the classes of the `card` component in the `dialog-card`, for example:

- `is-outlined`: To have no elevation and only a border.
- `elevation-{number}`: To give an elevation effect.

:::info
For more information, visit the [card component](./card) documentation.
:::

### Outlined

<Tabs>
  <TabItem value="preview" label="Preview">
    <PreviewElement>
      <div className="dialog-preview">
        <DialogOutlined />
      </div>
    </PreviewElement>
  </TabItem>

  <TabItem value="code" label="Code">
    <CodeBlock language="html">{DialogOutlinedCode}</CodeBlock>
  </TabItem>
</Tabs>

### Image

<Tabs>
  <TabItem value="preview" label="Preview">
    <PreviewElement>
      <div className="dialog-preview">
        <DialogImage />
      </div>
    </PreviewElement>
  </TabItem>

  <TabItem value="code" label="Code">
    <CodeBlock language="html">{DialogImageCode}</CodeBlock>
  </TabItem>
</Tabs>
