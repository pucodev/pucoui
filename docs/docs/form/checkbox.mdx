import PreviewElement from '@site/src/components/PreviewElement'
import CodeBlock from '@theme/CodeBlock'
import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'

import CheckboxBasic from '@site/src/components/html/form/checkbox/CheckboxBasic'
import CheckboxBasicCode from '@site/src/components/generated/form/checkbox/CheckboxBasic.html?raw'

# Checkbox

An input component that allows the user to select or deselect a binary option, such as true/false or on/off.

## Basic Usage

To implement a checkbox, use the `input` element with type `checkbox`. Additionally, the `field` and `checkbox` classes should be used as container classes.

<PreviewElement>
  <CheckboxBasic />
</PreviewElement>

<CodeBlock language="html">{CheckboxBasicCode}</CodeBlock>
