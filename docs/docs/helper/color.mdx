import { colors } from '@site/src/components/variables'
import PreviewElement from '@site/src/components/PreviewElement'
import CodeBlock from '@theme/CodeBlock'

# Colors

Set the text and background colors of your application's elements.

:::info

To see the available colors, visit the [customize colors documentation](../customize/color).

:::

## Text Color

<table>
  <thead>
    <tr>
      <th>Color</th>
      <th>Class</th>
      <th>Preview</th>
    </tr>
  </thead>
  <tbody>
    {colors.map((color, idx) => (
      <tr>
        <td>{color}</td>
        <td>
          <CodeBlock>is-color-{color}</CodeBlock>
        </td>
        <td>
          <PreviewElement>
            <div className={`is-color-${color}`}>Text</div>
          </PreviewElement>
        </td>
      </tr>
    ))}
  </tbody>
</table>

## Background Color

<table>
  <thead>
    <tr>
      <th>Color</th>
      <th>Class</th>
      <th>Preview</th>
    </tr>
  </thead>
  <tbody>
    {colors.map((color, idx) => (
      <tr>
        <td>{color}</td>
        <td>
          <CodeBlock>is-bg-{color}</CodeBlock>
        </td>
        <td>
          <PreviewElement>
            <div
              className={`is-bg-${color}`}
              style={{ width: '80px', height: '80px', borderRadius: '4px' }}
            ></div>
          </PreviewElement>
        </td>
      </tr>
    ))}
  </tbody>
</table>